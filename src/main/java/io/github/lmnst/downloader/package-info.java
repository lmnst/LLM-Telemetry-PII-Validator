/**
 * Parallel range-GET file downloader. Public entry points:
 * {@link io.github.lmnst.downloader.Downloader} for the library API and
 * {@link io.github.lmnst.downloader.cli.Main} for the CLI.
 *
 * <p>The package is {@code @NullMarked}: every type in it is non-null by
 * default unless explicitly annotated {@code @Nullable}.
 */
@org.jspecify.annotations.NullMarked
package io.github.lmnst.downloader;
